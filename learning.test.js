function x (f) {
    return x;
  }
  describe('', () => {
    it('x', () => {
      expect(pl(1,'cat')).toBe('1 cat');
    })
    it('y', () => {
      expect(pl(2,'cat')).toBe('2 cats');
    })
  });